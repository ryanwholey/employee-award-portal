---
version: "3.3"

services:
  
  db:
    image: mysql:5.7
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=true

  api:
    build:
      context: ../ 
    ports:
      - "4000:4000"
    env_file:
      - ../.env